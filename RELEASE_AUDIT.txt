BANJOFY 006.4.0 — MODULE 17 INTEGRATION BUILD 017
RELEASE-GATE AUDIT

BUILD TYPE
Complete clean rebuild from confirmed Build 002. No patch script.

CONFIRMED BUILD 016 EVIDENCE
Plugin staging passed and printed the exact three required bgutil modules.
The only failure was the expectation that a version-only yt-dlp command would
perform plugin/extractor initialisation.

ROOT-CAUSE CORRECTION
yt-dlp's own source shows that CLI parsing assigns plugin_dirs.value and calls
load_all_plugins before constructing YoutubeDL. Build 017 reproduces this exact
mechanism in the embedded downloader:
- assign portable root to yt_dlp.globals.plugin_dirs.value;
- call yt_dlp.plugins.load_all_plugins;
- import the three required provider modules.

WORKFLOW PROOF
- Exact installed distribution files staged: retained.
- Python API activation and module imports: added.
- `--version` plugin assertion: removed.
- `--verbose --list-extractors` initialisation proof: added.
- Portable artifact copy and existence checks: retained.

RUNTIME/PACKAGING
- Banjofy.exe-adjacent yt-dlp-plugins layout retained.
- Provider server and node_modules retained.
- Deno, EJS and FFmpeg unchanged.
- PyInstaller spec unchanged.
- Hidden console unchanged.
- Ten-second startup smoke test retained.

REPEAT
Build 016 repeat implementation retained unchanged:
- exclusive end boundary;
- locked timestamps;
- seek acknowledgement;
- post-count-in verification;
- locked end detection.

LOCAL PROOF
- Python compilation: passed.
- Exact workflow replacement: passed.
- Unsupported YoutubeDL plugin_dirs parameter absent: passed.
- Global plugin loader activation present: passed.
- Three explicit provider imports present: passed.
- Repeat deterministic tests: passed.
- ZIP integrity and checksums: passed.

GITHUB MUST STILL PROVE
- Windows-installed plugin files import through the yt-dlp loader;
- extractor initialisation reports the plugin directory;
- PyInstaller and frozen startup complete.

PETER'S WINDOWS TEST MUST STILL PROVE
- provider registration in the live diagnostic;
- YouTube format discovery and download;
- corrected repeat behaviour.

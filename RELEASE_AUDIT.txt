BANJOFY 006.4.0 — MODULE 17 INTEGRATION BUILD 019
RELEASE-GATE AUDIT

DIRECTION REVIEW
Question asked before implementation: is there a better route than continuing
to freeze the changing downloader stack inside Banjofy?

Answer: yes. Separate acquisition from the stable application.

KNOWN EARLIER FAILURE CLASS REMOVED
The active download route no longer imports yt-dlp, plugin namespaces, Deno or
the provider into the frozen Banjofy process. It launches a separately tested
official yt-dlp.exe from the Acquisition folder.

EAA CONTENTS
- official yt-dlp.exe 2026.06.09;
- Deno 2.8.1;
- FFmpeg staged from the pinned imageio-ffmpeg installation;
- bgutil PO-token provider 1.3.1 server and installed dependencies;
- exact bgutil yt_dlp_plugins files.

HANDOFF
Banjofy passes:
- selected URL;
- Library audio destination;
- output filename;
- Deno path;
- plugin path;
- provider path;
- FFmpeg path.

The EAA returns:
- process exit code;
- full diagnostic output;
- completed local audio path.

AUTOMATED PROOF
- all EAA executables exist;
- each executable starts;
- provider server and node_modules exist;
- exact plugin files exist;
- Banjofy health_check reports ready;
- Banjofy creates the expected EAA command;
- Python source compiles;
- chord and timing audits run;
- PyInstaller builds;
- normal GUI stays running for ten seconds;
- assembled EAA starts from final artifact layout;
- ZIP integrity and checksums pass.

LIMIT
A live YouTube request from Peter's network remains the final proof that
YouTube accepts the selected request. No build system can guarantee that a
third-party service will never change.

REPEAT
Build 018 repeat-boundary and seek-confirmation changes are retained unchanged.

CLEAN REBUILD
Created in a new empty directory from confirmed Build 002. Earlier baselines
remain untouched.

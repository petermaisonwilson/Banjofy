BANJOFY FIREFOX ACQUISITION LABORATORY 003 — RELEASE-GATE AUDIT

CONFIRMED BASELINE
- Laboratory 001 succeeded on Peter's Windows computer.
- Firefox downloaded the requested MP4 into the normal Downloads folder.
- The laboratory detected only the armed matching file and moved it into the selected test Library.

BUILD 003 SCOPE
- Preserve the proven watcher and title matching.
- Stage the completed file under <Library>\Working\Incoming.
- Validate actual media and duration using bundled FFmpeg.
- Require an audio stream.
- Present Keep audio only — Recommended, Keep video and audio, and Online playback only.
- Extract 192 kbps AAC M4A Practice audio for offline choices.
- Create a basic JSON song record under <Library>\Library\Songs.
- Store FFmpeg validation evidence under <Library>\Diagnostics.

DIRECT GITHUB PROOF
- Pinned imageio-ffmpeg 0.6.0 and PyInstaller 6.14.2.
- Generate a real two-second MP4 in the workflow.
- Extract a real M4A from it and assert both outputs exist and are non-empty.
- Build the EXE and hold it open for a ten-second startup smoke test.

ISOLATION
- Workflow runs only on firefox-acquisition-lab.
- Existing fa001.yml is overwritten rather than adding a second active workflow.
- No yt-dlp or internal YouTube downloader is included.
- Stable Banjofy source remains untouched.


BUILD 003 ROOT-CAUSE CORRECTION
- Build 002 created the temporary output as <name>.m4a.working.
- FFmpeg infers the output container from the final extension, so it saw .working and could not select an M4A muxer.
- Build 003 creates <name>.working.m4a, preserving the real .m4a extension.
- The GitHub gate now invokes the application's actual extract_practice_audio function, not a separate proxy command.
